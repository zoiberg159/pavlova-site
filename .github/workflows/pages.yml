# Публикация макета на GitHub Pages.
# Нужен, когда в Settings → Pages источником выбран «GitHub Actions»:
# сам по себе статический сайт оттуда не выкладывается, деплой делает этот воркфлоу.
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Параллельные деплои не нужны: важен последний, но начатый не обрываем.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          # Сборки нет, выкладываем корень репозитория как есть.
          path: '.'
      - id: deployment
        uses: actions/deploy-pages@v4
